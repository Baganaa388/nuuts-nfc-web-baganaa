# NFC Leaderboard - React + Vite + Node.js Migration

## Quick Start

### Prerequisites
- Node.js (v16 or higher)
- npm

### Installation & Running

1. **Install backend dependencies:**
```bash
cd backend
npm install
```

2. **Install frontend dependencies:**
```bash
cd frontend
npm install
```

3. **Start backend server:**
```bash
cd backend
npm start
# Runs on http://localhost:8000
```

4. **Start frontend dev server (in a new terminal):**
```bash
cd frontend
npm run dev
# Runs on http://localhost:5173
```

5. **Open browser:**
Navigate to `http://localhost:5173`

## Project Structure

- **`nfc-web/`** - Original project (read-only reference)
- **`backend/`** - Node.js + Express API server
- **`frontend/`** - React + Vite frontend application

## Core Features Preserved

✅ NFC UID registration and resolution  
✅ Leaderboard with real-time updates  
✅ User profiles  
✅ Admin dashboard  
✅ ESP32/Gateway API endpoints  
✅ Transaction management  
✅ All database operations  

See `MIGRATION_NOTES.md` for detailed migration documentation.

