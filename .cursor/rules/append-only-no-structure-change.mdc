---
alwaysApply: false
---

"prompt": [
      "You are collaborating on a shared GitHub repo with multiple teammates.",
      "STRICT RULE: Do NOT modify or delete existing lines of code. Append-only changes.",
      "",
      "What you MUST do:",
      "- Keep the current folder + file structure EXACTLY the same (no renames/moves).",
      "- Add new code by creating NEW files or NEW small components/functions.",
      "- When you need to alter behavior, write a thin wrapper/adapter in a NEW file and call it from an existing extension point (e.g., route registry, component slot, plugin list) WITHOUT editing unrelated code.",
      "- If integration is required (e.g., registering a route/component), only add the minimal line(s) in the ALREADY DESIGNATED registration spot. Do not refactor or rearrange surrounding code.",
      "- Preserve code style and existing imports; follow Tailwind-only styling if UI work is involved (no inline styles, no new CSS files).",
      "- Keep light/dark theme behavior intact when adding UI.",
      "- Keep NFC/UID/registration/leaderboard business logic UNCHANGED.",
      "- Keep dependency list unchanged (no new packages).",
      "- Keep build/test/lint configs unchanged.",
      "",
      "What you MUST NOT do:",
      "- Do NOT edit existing function bodies unless there is an explicit TODO marker allowing it.",
      "- Do NOT rename files, symbols, or directories.",
      "- Do NOT remove, reorder, or reformat large blocks of existing code.",
      "- Do NOT introduce new dependencies or change configs.",
      "- Do NOT change APIs, request/response shapes, DB schemas, or existing routes.",
      "",
      "If a change to existing code seems unavoidable:",
      "- Prefer composition over modification: create a NEW wrapper function/component and use it via existing extension points.",
      "- If absolutely necessary to touch an existing file, limit to the SMALLEST additive change (e.g., add one import + one line to register a new module) and DO NOT modify existing lines.",
      "",
      "Output quality:",
      "- New files must be small, focused, and well-named.",
      "- Add concise comments at the top describing purpose and integration point.",
      "- Ensure no dead code and no unused exports.",
      "- Ensure mobile-first responsiveness and theme tokens if UI is added.",
      "",
      "Deliver only the minimal new/added code needed. Do not include explanations in files."
    ]